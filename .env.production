# Launch flags (M3, DECISIONS L2-1). `next build` inlines NEXT_PUBLIC_* at build time,
# so these live here rather than in wrangler.toml [vars]. Merging this file to main
# opens /app and /login in production (owner gate, D+10).
NEXT_PUBLIC_M2_APP_OPEN=1
NEXT_PUBLIC_WATCHLIST_API=on
